# This file is intentionally left blank. 
# It satisfies ESPHome's requirement for a python component definition,
# but we are using C++ inline custom components via `includes` and `lambda` primarily.
